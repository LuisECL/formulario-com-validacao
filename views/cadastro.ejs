<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/usuario.css">
  <title>Cadastro de usuarios</title>
</head>
<body>
  <header>
    <div>Logotipo Genérico</div>
    <div>Info do usuário</div>
  </header>
  <main>
    <form action="/cadastro" method="POST">
      <h1>Cadastrar novo usuário</h1>

      <label for="nome">Nome: </label>
      <input type="text" id="nome" name="nome" value="<%= locals.old && old.nome %>">
      <% if (locals.errors && errors.nome) { %>
        <p class="feedback"><%= errors.nome.msg %></p>
      <% } %>
      <br>

      <label for="email">E-mail: </label>
      <input type="email" id="email" name="email" value="<%= locals.old && old.email %>">
      <% if (locals.errors && errors.email) { %>
        <p class="feedback"><%= errors.email.msg %></p>
      <% } %>
      <br>

      <label for="senha01">Senha: </label>
      <input type="password" id="senha01" name="senha01">
      <% if (locals.errors && errors.senha01) { %>
        <p class="feedback"><%= errors.senha01.msg %></p>
      <% } %>
      <br>

      <label for="senha02">Confirmar senha: </label>
      <input type="password" id="senha02" name="senha02">
      <% if (locals.errors && errors.senha02) { %>
        <p class="feedback"><%= errors.senha02.msg %></p>
      <% } %>
      <br>

      <button>Enviar</button>
    </form>
  </main>

  <footer>
    <div>Footer genérico</div>
    <div>Links para redes sociais</div>
  </footer>

</body>
</html>